# HF Spaces requirements (API mode — no llama-cpp-python or CUDA needed)
# CPU-only PyTorch for sentence-transformers embeddings
--extra-index-url https://download.pytorch.org/whl/cpu
gradio>=4.19.0
huggingface-hub>=0.20.0
beautifulsoup4>=4.12.0
requests>=2.31.0
rich>=13.7.0
click>=8.1.0
pyyaml>=6.0
psutil>=5.9.0

# RAG pipeline (embeddings + vector DB)
sentence-transformers>=2.5.0,<3.0
transformers>=4.41.0,<5.0
lancedb>=0.6.0

# LLM orchestration
langchain>=0.1.0
langchain-community>=0.0.20
langchain-text-splitters>=0.0.1
